export const API_PREFIX = {
  EVENTS: '/api/events',
  MARKETS: '/api/markets',
};

export const API_PATH = {
  SEARCH_EVENTS: '/search/:search',
};

export enum API_METHOD {
  post = 'post',
  get = 'get',
  put = 'put',
}

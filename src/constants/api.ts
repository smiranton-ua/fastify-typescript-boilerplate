export const API_PREFIX = {
  EVENTS: '/api/events',
  MARKETS: '/api/markets',
};

export const API_PATH = {
  SEARCH_EVENTS: '/search/:params',
};

export enum API_METHOD {
  post = 'post',
  get = 'get',
  put = 'put',
}
